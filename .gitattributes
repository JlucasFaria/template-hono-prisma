# Normalize line endings to LF for all text files.
# This prevents CRLF/LF conflicts on Windows where core.autocrlf=true
# causes files to appear modified after running Prettier (which writes LF).
* text=auto eol=lf
